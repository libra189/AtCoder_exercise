services:
  atcoder:
    image: atcoder:0.1.0
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
      args:
        REQUIREMENTS: .devcontainer/requirements.txt
    tty: true
    volumes:
      - .:/app
